// Interface for the route components.
export interface IRouteComponent {
    classes: any;
}

// Interface for the Bar component.
export interface IAppBarComponent {
    classes: any;
    children: any;
}