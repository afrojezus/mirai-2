import * as types from '../mutation-types';

const mainState = {
  notification: {}
};

const actions = {};

const mainGetters = {};

const mutations = {};

export default {
  state: mainState,
  getters: mainGetters,
  actions,
  mutations,
  initialState: JSON.parse(JSON.stringify(mainState)),
}
